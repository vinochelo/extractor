# Firebase Client SDK Configuration (shared with the browser)
# Get these from your Firebase project settings > General
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"

# Firebase Admin SDK Configuration (server-side only)
# Generate a new private key in your Firebase project settings > Service accounts
# It's recommended to use a service account with limited permissions.
FIREBASE_CLIENT_EMAIL="your-service-account-email"
# Copy the private key from the JSON file, and ensure newlines are handled correctly.
# One way is to replace all newlines with \n and wrap the key in quotes.
FIREBASE_PRIVATE_KEY="your-private-key"
